<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ── -->
    <!-- Camera for Vision & Navigation modules -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone for Audio module -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Location for Navigation module -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet for transit APIs and map tiles -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Vibration for haptic alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Foreground service for continuous sound monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ── Hardware features ── -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".AccessAIApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="HelpSense"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AccessAI"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AccessAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Audio monitoring foreground service (Step 2) -->
        <!--
        <service
            android:name=".service.AudioMonitorService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
        -->

    </application>

</manifest>
